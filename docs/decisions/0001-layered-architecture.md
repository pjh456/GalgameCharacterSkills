# 0001 分层架构

## 背景

项目已经从单体脚本式实现逐步演进为多目录结构，需要明确分层边界以降低继续演化时的耦合。

## 决策

采用：

- `routes -> api -> application -> domain`

并辅以：

- `gateways`
- `checkpoint`
- `llm`
- `files`
- `workspace`

作为支撑子系统。

## 影响

- 请求入口、业务流程、基础设施边界更清晰
- 新代码需要遵循分层约束

## 备注

这不是严格六边形架构，但已经具备明确依赖方向。
